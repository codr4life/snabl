#ifndef SNABL_TYPES_HPP
#define SNABL_TYPES_HPP

#include "snabl/std.hpp"

namespace snabl {
	using Char = unsigned char;
	using Int = long long;
	using Str = string;
	using StrPtr = shared_ptr<Str>;
}

#endif
